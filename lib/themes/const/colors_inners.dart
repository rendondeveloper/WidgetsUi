import 'package:flutter/material.dart';

//Toolbar
const basePrimary = Color(0xff08FFFF);
//El color de los iconos de la toolbar
const baseOnPrimary = Color(0xffFFFFFF);
//Floating action buttons
//Selection controls, like sliders and switches
//Highlighting selected text
//Progress bars
//Links and headlines
const baseSecondary = Color(0x7008FFFF);
//El color de los iconos de los botones flotantes
const baseOnSecondary = Color(0xFF212121);
//El color de los iconos de los botones flotantes
const baseOnSecondaryWithOpacity = Color(0x35212121);
//El colors affect surfaces of components, such as cards, sheets, and menus.
const baseSurface = Color(0xff08FFFF);
//El background icons into cards
const baseOnSurface = Color(0xFF212121);
//El color appears behind scrollable content. The baseline background and surface color is #FFFFFF.
const baseBackground = Color(0xffFFFFFF);
//El color text on top screen
const baseOnBackground = Color(0xFF212121);
//El color indicates errors in components, such as invalid text in a text field. The baseline error color is #B00020.
const baseError = Color(0xffFF5722);
//El color icons into errors cards or components
const baseOnError = Color(0xffFFFFFF);
//El color to disable elements
const baseDisableElement = Color(0xffBDBDBD);
//El color to icons into disable elements
const baseOnDisableElement = Color(0xFF212121);
//El color to icons into disable elements
const baseDivider = Color(0xffBDBDBD);
